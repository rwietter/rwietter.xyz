/* eslint-disable react/no-unescaped-entities */
import { Header, Title } from './styles';

const TITLE = 'Trabalho, hobby & open source.';

export const ProjectHeader = () => (
  <Header>
    <Title>{TITLE}</Title>
  </Header>
);

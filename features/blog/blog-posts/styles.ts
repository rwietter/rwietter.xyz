import { styled } from 'features/ui/theme';

export const ArticlesContainer = styled('ul', {
  display: 'flex',
  flexDirection: 'column',
  padding: '3rem 1.5rem',
  listStyle: 'none',
  margin: 0,
});
